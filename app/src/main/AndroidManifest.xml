<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TourFranceAndroid"
        android:usesCleartextTraffic="true">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_MAPS_API_KEY" />

        <activity
            android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.CyclistListActivity" />
        <activity android:name=".view.CyclistFormActivity" />

        <activity android:name=".view.TeamListActivity" />
        <activity android:name=".view.TeamFormActivity" />

        <activity android:name=".view.StageListActivity" />

        <activity android:name=".view.ClimbListActivity" />

        <activity android:name=".view.FavouriteListActivity" />
        <activity android:name=".view.FavouriteFormActivity" />

        <activity android:name=".view.SettingsActivity" />

    </application>

</manifest>